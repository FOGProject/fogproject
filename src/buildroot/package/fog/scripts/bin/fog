#!/bin/bash
. /usr/share/fog/lib/funcs.sh
case "$type" in
    down)
        fog.download
        ;;
    up)
        fog.upload
        ;;
    *)
        if [[ -z $type ]]; then
            type="Null"
        fi
        handleError "Fatal Error: Unknown request type :: $type"
        ;;
esac
