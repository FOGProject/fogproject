#!/bin/bash
res=""
while [[ $res == "#!OK" ]]; do
    res=$(wget -qO - http://${web}service/donate.php 2>/dev/null)
    usleep 60000000
done
reset
clear
echo
echo $res
echo
usleep 10000000
$afterActionTerm
