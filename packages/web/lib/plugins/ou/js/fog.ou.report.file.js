(function($) {

    reportString = window.atob(Common.f);

    // This will call our respective calls
    // to report the requested data.
    switch (reportString) {
        // Equipment Loan
        case 'ou report':
            break;
    }
})(jQuery);
